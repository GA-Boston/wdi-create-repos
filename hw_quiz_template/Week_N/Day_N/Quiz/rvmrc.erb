if [[ -d "${rvm_path:-$HOME/.rvm}/environments" && \
      -s "${rvm_path:-$HOME/.rvm}/environments/ruby-2.0.0@<%=repo_name%>" ]] ; then 
  \. "${rvm_path:-$HOME/.rvm}/environments/ruby-2.0.0@<%=repo_name%>"
else 
rvm --create use  "ruby-2.0.0@<%=repo_name%>"    
fi
bundle